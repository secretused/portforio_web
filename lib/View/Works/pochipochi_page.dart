import 'package:flutter/material.dart';

class Pochipochi extends StatelessWidget {
  const Pochipochi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: Center(
        child: Text("PochiPochi"),
      ),
    );
  }
}
