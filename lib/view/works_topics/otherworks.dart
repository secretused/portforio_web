import 'package:flutter/material.dart';

class OtherWorks extends StatelessWidget {
  const OtherWorks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: Center(
        child: Text("otherworks"),
      ),
    );
  }
}
